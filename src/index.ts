import { default as createElement } from './createElement';
import { default as render, useState } from './render';

const Treact = {
  createElement,
  render,
  useState,
};

export default Treact;
